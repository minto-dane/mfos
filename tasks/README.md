# tasks/

Task and roadmap bridge. Current work breakdown and roadmap material is in
[docs/design/tasks](../docs/design/tasks).

Historical phase-specific task plans are kept under `archive/<phase>/`.
Current task roots must use stable names and be listed in `index.yml`.
