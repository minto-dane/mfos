# fuzz/

Fuzz targets, corpora, and dictionaries. Parsers for DSN, JCL-like input,
operator commands, policy language, update manifests, AMF manifests, and ABI
frames require fuzz targets unless explicitly waived in a spec.

