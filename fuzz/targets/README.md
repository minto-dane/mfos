# fuzz/targets/

Current fuzz target planning artifacts and future target directories.

Phase-specific plans belong under `archive/<phase>/`. The stable current plan
is `fuzz-target-plan.yml`. No fuzz implementation is stored here during Phase
0.9.
