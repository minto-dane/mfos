# Traceability Evidence

This directory contains machine-readable traceability matrices.

- `current/` contains stable current traceability matrices.
- `generated/` contains deterministic generator outputs grouped by context.
- `archive/` contains historical phase-specific traceability.

Use `index.yml` as the machine-readable inventory. New phase-specific files must
be placed under a phase directory instead of this root.
