# schemas/

Executable JSON Schema definitions live here. CUE definitions are not present
yet. Custom validation logic lives under `scripts/validators/` and
`scripts/checks/`.
Current schema notes are in Markdown/YAML under `docs/design` and bridge files.
