# schemas/mfos/

MFOS-owned schema definitions used by local validation tooling.

This directory is validation-only. Schema presence does not authorize
production implementation, hosted daemons, Rust semantic-core work, Dafny
executable semantics, or a semantic runner.
