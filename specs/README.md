# specs/

Top-level bridge for canonical MFOS specifications.

The current v0.5 split specs are under:

```text
docs/design/specs/
```

This directory is reserved for a future promotion of specs into a top-level
canonical layout. Until then, do not create parallel normative specs here.

