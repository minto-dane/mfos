# Docs / Spec Scaffold Audit

Canonical spec path: `docs/design/specs/`

Top-level `specs/` is a bridge/planned promotion target.

Split specs checked: `47`

Missing front matter: `0`

Thin specs under 80 lines: `0`

`docs/design/STATUS.md` now references artifact indexes instead of acting as the detailed artifact inventory.

Recommended action: keep `docs/design/specs/` canonical until a canonical-path ADR moves specs to top-level `specs/`. Specs `36` through `41` are deferred draft scaffolds only; they do not authorize implementation.
