# Artifact Lifecycle Open Issues

Status: current.

These issues are deferred follow-ups. They do not permit production or Phase
1.4 implementation work.

## Deferred Medium

- Expand per-file manifests for `tests/fixtures/` and `tests/golden/`; current
  indexes are directory-level.
- Add per-file archive metadata for historical traceability and test catalogs.
- Refresh source-matrix parity for the populated `sources/` workbench before any
  future migration ADR.
- Normalize Phase 0.10 planning catalogs with fuller lifecycle headers before
  implementation assignment.
- Add fuzz corpus inventory before fuzz implementation assignment.

## Deferred Low

- Add supersession maps for older Phase 0.8 fuzz target names.
- Normalize old archived task documents so historical status and archived status
  are distinct.
