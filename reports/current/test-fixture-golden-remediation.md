# Test, Fixture, and Golden Vector Remediation

Status: current

Phase 0.9 test catalogs, fixtures, oracles, golden vectors, and fuzz plans
remain unchanged semantically. Existing validators still treat these artifacts
as deterministic executable-spec inputs, not implemented tests or a semantic
runner.

Known follow-up: fixtures and golden vectors still need direct source-ref
propagation before semantic evaluator implementation is allowed.
