# Spec and Documentation Remediation

Status: current

Specs `36` through `41` were added as draft scaffolds and listed in
`docs/design/specs/INDEX.md`. They are planning boundaries only and do not
authorize hypervisor, confidential workload, cluster, semantic-runner,
production service, or proof implementation work.

The spec index now covers `00` through `41` and keeps `spec_id` as the durable
key while historical number collisions remain documented.
