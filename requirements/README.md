# requirements/

Top-level bridge for machine-readable requirements.

The current v0.5 priority requirements catalog is:

```text
docs/design/registries/requirements.yaml
```

Future full-domain requirements can be split under `requirements/by-domain/`
only after a schema-backed migration keeps the existing catalog in sync.

