# requirements/by-domain/

Reserved for the future full split of requirements by namespace. The current
canonical v0.5 priority registry remains
`docs/design/registries/requirements.yaml`.

This bridge is validation-only. It does not authorize production code, hosted
daemons, Rust semantic-core work, Dafny executable semantics, or a semantic
runner.
