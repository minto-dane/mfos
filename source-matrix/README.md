# source-matrix/

Top-level bridge for Source Matrix work.

The current canonical v0.5 Source Card index is:

```text
docs/design/source-matrix/source-matrix.yml
```

The current Source Cards are:

```text
docs/design/source-matrix/cards/*.yml
```

Do not create a competing ledger here until a migration ADR moves the canonical
source matrix out of `docs/design`.

