# nucleus/

Future top-level scaffold for MFOS nucleus work.

This directory is metadata-only. It does not authorize production nucleus code,
hosted daemons, Rust semantic-core work, Dafny executable semantics, semantic
runner work, or implementation files.
