# guard/

Future top-level scaffold for MFOS Guard work.

This directory is metadata-only. It does not authorize production Guard code,
hosted daemons, Rust semantic-core work, Dafny executable semantics, semantic
runner work, or implementation files.
