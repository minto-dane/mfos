# TLA+ Model Scaffold

Status: planned scaffold.

This directory is reserved for future normalized references to MFOS TLA+ model
artifacts. Existing Phase 0.x TLA+ design models remain under `formal/tla/`
until a reviewed migration updates the formal model registry.

This directory does not authorize production implementation, semantic runner
implementation, hosted daemon implementation, or runtime behavior.
