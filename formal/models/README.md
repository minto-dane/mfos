# formal/models/

Reserved for component-level formal model metadata and indexes.

The current formal assurance registry remains `formal/registry.yml`, with model
entries in `formal/model-registry.yml`. This directory does not authorize
production code, a Rust semantic core, Dafny executable semantics, or a semantic
runner.
