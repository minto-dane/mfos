# Test Fixtures

This directory contains deterministic fixture artifacts grouped by MFOS domain.

The existing domain directories are the current fixture inventory. Do not add
phase-specific files directly under this root; use `archive/<phase>/` for
historical fixtures if needed.

Use `index.yml` as the machine-readable inventory.
