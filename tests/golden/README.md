# Golden Vectors

This directory contains deterministic golden vectors grouped by MFOS domain.

The existing domain directories are the current golden-vector inventory. Do not
add phase-specific files directly under this root; use `archive/<phase>/` for
historical vectors if needed.

Use `index.yml` as the machine-readable inventory.
