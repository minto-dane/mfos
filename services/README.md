# services/

Future top-level scaffold for MFOS service components.

This directory is metadata-only. It does not authorize production services,
hosted daemons, Rust semantic-core work, Dafny executable semantics, semantic
runner work, or implementation files.
