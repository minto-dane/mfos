# pxm/

Future top-level scaffold for MFOS PXM work.

This directory is metadata-only. It does not authorize production PXM code,
hosted daemons, Rust semantic-core work, Dafny executable semantics, semantic
runner work, or implementation files.
